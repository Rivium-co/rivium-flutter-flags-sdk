name: rivium_flutter_flags
description: Feature Flags SDK for Flutter - Lightweight feature flag management with Rivium. Pure Dart, works on all platforms.
version: 0.1.0
homepage: https://rivium.co
repository: https://github.com/Rivium-co/rivium-flutter-flags-sdk

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  crypto: ^3.0.3
  shared_preferences: ^2.2.0
  connectivity_plus: ^6.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
